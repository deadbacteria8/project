
const handleProjectData = (data) => {
    if(data.customized) {
        customizedData(data);
    }
}

function customizedData(data) {
    const startDate = dateRange[0];
    const endDate = dateRange[1];
    let last = endDate;
    while(true) {
        //if last is before startdate, break
        
    }
}

module.exports = handleProjectData;